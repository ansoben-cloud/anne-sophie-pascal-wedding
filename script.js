const slides=[...document.querySelectorAll('.slide')];const dots=[...document.querySelectorAll('.dot')];const prevBtn=document.getElementById('prev-btn');const nextBtn=document.getElementById('next-btn');const langButtons=[...document.querySelectorAll('.lang-btn')];const form=document.getElementById('rsvp-form');const guestFields=document.getElementById('guest-fields');const companionsField=document.getElementById('companions-field');const guestCount=form.elements.guestCount;const companions=form.elements.companions;const successPanel=document.getElementById('success-panel');const formHeading=document.getElementById('form-heading');let currentSlide=0,currentLang='fr';
function showSlide(index){currentSlide=Math.max(0,Math.min(slides.length-1,index));slides.forEach((s,i)=>s.classList.toggle('active',i===currentSlide));dots.forEach((d,i)=>d.classList.toggle('active',i===currentSlide));prevBtn.disabled=currentSlide===0;nextBtn.disabled=currentSlide===slides.length-1;document.querySelector('.viewer').scrollTop=0}
function setLanguage(lang){currentLang=lang;const he=lang==='he';document.documentElement.lang=he?'he':'fr';document.documentElement.dir=he?'rtl':'ltr';langButtons.forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));document.querySelectorAll('img[data-fr]').forEach(img=>img.src=img.dataset[lang])}
prevBtn.addEventListener('click',()=>showSlide(currentSlide-1));nextBtn.addEventListener('click',()=>showSlide(currentSlide+1));dots.forEach(d=>d.addEventListener('click',()=>showSlide(Number(d.dataset.go))));langButtons.forEach(b=>b.addEventListener('click',()=>setLanguage(b.dataset.lang)));document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')showSlide(currentSlide+(currentLang==='he'?-1:1));if(e.key==='ArrowLeft')showSlide(currentSlide+(currentLang==='he'?1:-1))});
form.addEventListener('change',e=>{if(e.target.name==='attendance'){const attending=e.target.value==='yes';guestFields.classList.toggle('hidden',!attending);guestCount.required=attending;if(!attending){guestCount.value='';companions.value='';companions.required=false;companionsField.classList.add('hidden')}}if(e.target.name==='guestCount'){const needs=Number(e.target.value)>1;companionsField.classList.toggle('hidden',!needs);companions.required=needs;if(!needs)companions.value=''}});
form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return}form.classList.add('hidden');formHeading.classList.add('hidden');successPanel.classList.remove('hidden')});showSlide(0);setLanguage('fr');
